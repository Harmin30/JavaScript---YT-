// let headings = document.getElementsByClassName("heading-class");
// console.dir(headings);
// console.log(headings);

let firstEl = document.querySelector(".heading"); //Gets the first element 
console.dir(firstEl);

let allEl = document.querySelectorAll(".heading"); //Gets all the elements
console.dir(allEl)